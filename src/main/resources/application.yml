spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/jpatestdb"
    username: "root"
    password: "asdasd12"
  h2:
    console:
      enabled: true
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: validate
    generate-ddl: true

server:
  port: 8080